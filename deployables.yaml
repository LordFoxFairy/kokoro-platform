schemaVersion: 1
image:
  buildRoot: .
  owner: platform
deployables:
  - id: platform-api
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-api
    processRole: api
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_API_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-api
      permissions: bounded-transactional-api
      ddl: forbidden
    activationAuthorized: false
    runtimeTraffic: false
    inboundContracts: []
    declaredInboundContracts: [platform-public-http]
    outboundContracts: []
    secretClasses:
      - platform-api-database
      - product-workload-registry
      - session-access-signing-keyring
      - authorization-event-signing-keyring
      - mtls-server
      - mtls-client-ca
      - commerce-redemption-keyring
      - asset-upload-policy-registry
      - asset-upload-capability-keyring
      - artifact-delivery-capability-hmac-key
      - artifact-owner-cursor-hmac-key
      - identity-password-pepper-keyring
      - identity-verification-digest-key
      - identity-session-digest-key
      - identity-refresh-digest-key
      - identity-reauthentication-digest-key
      - identity-audit-digest-key
      - identity-delivery-encryption-key
      - identity-totp-encryption-keyring
    ports:
      - { name: public, containerPort: 4100, protocol: https, exposure: service, mtls: true }
      - { name: health, containerPort: 4101, protocol: http, exposure: pod-only, mtls: false }
    fileContract:
      trustRootEnvironment: PLATFORM_API_FILE_TRUST_ROOT
      mountPath: /run/secrets/platform-api
      defaultMode: "0440"
      requiredEnvironment:
        - PLATFORM_PRODUCT_WORKLOAD_REGISTRY_FILE
        - PLATFORM_SESSION_ACCESS_KEY_RING_FILE
        - PLATFORM_AUTHORIZATION_EVENT_KEY_RING_FILE
        - PLATFORM_PUBLIC_TLS_KEY_FILE
        - PLATFORM_PUBLIC_TLS_CERT_FILE
        - PLATFORM_PUBLIC_TLS_CLIENT_CA_FILE
        - PLATFORM_COMMERCE_REDEMPTION_KEY_RING_FILE
        - PLATFORM_ASSET_UPLOAD_POLICY_REGISTRY_FILE
        - PLATFORM_ASSET_UPLOAD_CAPABILITY_KEY_RING_FILE
        - PLATFORM_ARTIFACT_DELIVERY_CAPABILITY_KEY_FILE
        - PLATFORM_ARTIFACT_OWNER_CURSOR_KEY_FILE
        - PLATFORM_IDENTITY_PASSWORD_PEPPER_RING_FILE
        - PLATFORM_IDENTITY_VERIFICATION_DIGEST_KEY_FILE
        - PLATFORM_IDENTITY_SESSION_DIGEST_KEY_FILE
        - PLATFORM_IDENTITY_REFRESH_DIGEST_KEY_FILE
        - PLATFORM_IDENTITY_REAUTH_DIGEST_KEY_FILE
        - PLATFORM_IDENTITY_AUDIT_DIGEST_KEY_FILE
        - PLATFORM_IDENTITY_DELIVERY_KEY_FILE
        - PLATFORM_IDENTITY_TOTP_KEY_RING_FILE
    probes:
      liveness: { path: /health/live, port: health, scheme: HTTP }
      readiness: { path: /health/ready, port: health, scheme: HTTP }
    audience: platform-api
    scalingKey: http-concurrency
    readiness: /health/ready
    drain: http-graceful
    sloOwner: platform
    runbookOwner: platform
    releaseOwner: platform

  - id: platform-admission
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-admission
    processRole: admission
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_ADMISSION_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-admission
      identityLifecycle: run-scoped-leased-role
      permissions: admission-owner-transaction-only
      ddl: forbidden
    activationAuthorized: false
    runtimeTraffic: false
    inboundContracts: []
    declaredInboundContracts: [platform-admission-connect, platform-asset-eligibility-connect]
    outboundContracts:
      [session-admission-owner-connect, session-dispatch-owner-evidence-connect, agent-execution-evidence-connect]
    secretClasses:
      [
        platform-admission-database,
        mtls-server,
        mtls-peer-registry,
        mtls-client,
        session-peer-ca,
        agent-peer-ca,
        admission-hpke-public-key-ring,
      ]
    audience: platform-admission
    scalingKey: connect-request-concurrency
    readiness: /health/ready
    drain: http2-graceful
    sloOwner: platform
    runbookOwner: platform
    releaseOwner: platform

  - id: platform-commerce-worker
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-commerce-worker
    processRole: commerce-worker
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_COMMERCE_WORKER_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: commerce-worker
      permissions: commerce-outbox-consumer-only
      ddl: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    requiredEnvironment:
      [DATABASE_URL_PLATFORM, PLATFORM_DATABASE_CREDENTIAL_CLASS,
       PLATFORM_DATABASE_EXPECTED_DATABASE, PLATFORM_DATABASE_MIGRATOR_ROLE,
       PLATFORM_DATABASE_COMMERCE_WORKER_ROLE, PLATFORM_WORKER_ID,
       PLATFORM_OUTBOX_DELIVERY_ENDPOINT, PLATFORM_OUTBOX_DELIVERY_KEY_ID,
       PLATFORM_COMMERCE_WORKER_SECRET_TRUST_ROOT,
       PLATFORM_COMMERCE_OUTBOX_DELIVERY_SECRET_FILE]
    outboundContracts: [commerce-credit-outbox-delivery-https]
    secretClasses: [platform-commerce-worker-database, commerce-credit-outbox-delivery-hmac-key]
    audience: platform-commerce-worker
    scalingKey: commerce-outbox-lag
    readiness: process-state
    drain: stop-commerce-claim-then-return-lease
    sloOwner: platform-commerce
    runbookOwner: platform-commerce
    releaseOwner: platform

  - id: platform-site-worker
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-site-worker
    processRole: site-worker
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_SITE_WORKER_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: site-worker
      permissions: site-runtime-consumer-only
      ddl: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    requiredEnvironment:
      [DATABASE_URL_PLATFORM, PLATFORM_DATABASE_CREDENTIAL_CLASS,
       PLATFORM_DATABASE_EXPECTED_DATABASE, PLATFORM_DATABASE_MIGRATOR_ROLE,
       PLATFORM_DATABASE_SITE_WORKER_ROLE, PLATFORM_WORKER_ID,
       PLATFORM_SITE_PROVIDER_REGISTRY_FILE, PLATFORM_AUTHORIZATION_EVENT_KEY_RING_FILE]
    outboundContracts: [site-deployment-provider-https]
    secretClasses: [platform-site-worker-database, authorization-event-signing-keyring,
      site-provider-registry, site-provider-bearer-tokens]
    audience: platform-site-worker
    scalingKey: site-runtime-outbox-lag
    readiness: process-state
    drain: stop-site-claim-then-return-lease
    sloOwner: platform-site
    runbookOwner: platform-site
    releaseOwner: platform

  - id: platform-asset-worker
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-asset-worker
    processRole: asset-worker
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_ASSET_WORKER_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: asset-worker
      permissions: asset-effect-consumer-only
      ddl: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    requiredEnvironment:
      [DATABASE_URL_PLATFORM, PLATFORM_DATABASE_CREDENTIAL_CLASS,
       PLATFORM_DATABASE_EXPECTED_DATABASE, PLATFORM_DATABASE_MIGRATOR_ROLE,
       PLATFORM_DATABASE_ASSET_WORKER_ROLE, PLATFORM_WORKER_ID, PLATFORM_ENVIRONMENT,
       PLATFORM_REGION, PLATFORM_ASSET_WORKER_SECRET_TRUST_ROOT,
       PLATFORM_ASSET_STORAGE_ROUTE_FILE, PLATFORM_ASSET_INSPECTION_POLICY_REGISTRY_FILE,
       PLATFORM_ASSET_SCANNER_ENDPOINT, PLATFORM_ASSET_SCANNER_AUDIENCE,
       PLATFORM_ASSET_SCANNER_TOKEN_FILE, PLATFORM_ASSET_SCANNER_TLS_CA_FILE,
       PLATFORM_ASSET_SCANNER_TLS_CERT_FILE, PLATFORM_ASSET_SCANNER_TLS_KEY_FILE]
    outboundContracts: [asset-security-scanner-mtls, s3-object-api]
    secretClasses: [platform-asset-worker-database, asset-storage-route-registry,
      asset-scanner-bearer-token, asset-scanner-mtls-client]
    audience: platform-asset-worker
    scalingKey: asset-outbox-lag
    readiness: process-state
    drain: stop-asset-claim-then-return-lease
    sloOwner: platform-asset
    runbookOwner: platform-asset
    releaseOwner: platform

  - id: platform-admin-worker
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-admin-worker
    processRole: admin-worker
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_ADMIN_WORKER_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: admin-worker
      permissions: admin-execution-consumer-only
      ddl: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    requiredEnvironment:
      [DATABASE_URL_PLATFORM, PLATFORM_DATABASE_CREDENTIAL_CLASS,
       PLATFORM_DATABASE_EXPECTED_DATABASE, PLATFORM_DATABASE_MIGRATOR_ROLE,
       PLATFORM_DATABASE_ADMIN_WORKER_ROLE, PLATFORM_WORKER_ID]
    outboundContracts: []
    secretClasses: [platform-admin-worker-database]
    audience: platform-admin-worker
    scalingKey: admin-execution-lag
    readiness: process-state
    drain: stop-admin-claim-then-return-lease
    sloOwner: platform-admin
    runbookOwner: platform-admin
    releaseOwner: platform

  - id: platform-authorization-maintenance
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-authorization-maintenance
    processRole: authorization-maintenance
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_AUTHORIZATION_MAINTENANCE_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: authorization-maintenance
      permissions: authorization-retention-only
      ddl: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    requiredEnvironment:
      [DATABASE_URL_PLATFORM, PLATFORM_DATABASE_CREDENTIAL_CLASS,
       PLATFORM_DATABASE_EXPECTED_DATABASE, PLATFORM_DATABASE_MIGRATOR_ROLE,
       PLATFORM_DATABASE_AUTHORIZATION_MAINTENANCE_ROLE]
    outboundContracts: []
    secretClasses: [platform-authorization-maintenance-database]
    audience: platform-authorization-maintenance
    scalingKey: cron-schedule
    readiness: run-to-completion
    drain: abort-transaction
    sloOwner: platform-authorization
    runbookOwner: platform-authorization
    releaseOwner: platform

  - id: platform-identity-worker
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-identity-worker
    processRole: identity-worker
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_IDENTITY_WORKER_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-identity-worker
      permissions: identity-outbox-consumer-only
      ddl: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    requiredEnvironment:
      [DATABASE_URL_PLATFORM, PLATFORM_DATABASE_CREDENTIAL_CLASS,
       PLATFORM_DATABASE_EXPECTED_DATABASE, PLATFORM_DATABASE_MIGRATOR_ROLE,
       PLATFORM_DATABASE_IDENTITY_WORKER_ROLE, PLATFORM_WORKER_ID,
       PLATFORM_IDENTITY_AUDIT_DIGEST_KEY_FILE, PLATFORM_IDENTITY_SECRET_TRUST_ROOT,
       PLATFORM_IDENTITY_DELIVERY_ENDPOINT, PLATFORM_IDENTITY_DELIVERY_HMAC_KEY_ID,
       PLATFORM_IDENTITY_DELIVERY_HMAC_SECRET_FILE]
    outboundContracts: [identity-verification-delivery-https]
    secretClasses:
      [platform-identity-worker-database, identity-audit-digest-key,
       identity-delivery-hmac-key]
    audience: platform-identity-worker
    scalingKey: identity-outbox-lag
    readiness: process-state
    drain: stop-identity-claim-then-return-lease
    sloOwner: platform-identity
    runbookOwner: platform-identity
    releaseOwner: platform

  - id: platform-media-worker
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-media-worker
    processRole: media-worker
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_MEDIA_WORKER_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: media-worker
      permissions: media-worker-function-only
      ddl: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    requiredEnvironment:
      [DATABASE_URL_PLATFORM, PLATFORM_DATABASE_CREDENTIAL_CLASS,
       PLATFORM_DATABASE_EXPECTED_DATABASE, PLATFORM_DATABASE_MIGRATOR_ROLE,
       PLATFORM_DATABASE_MEDIA_WORKER_ROLE, PLATFORM_WORKER_ID,
       PLATFORM_MEDIA_WORKER_SECRET_TRUST_ROOT, PLATFORM_MEDIA_INPUT_KEY_RING_FILE,
       PLATFORM_MEDIA_CAPABILITY_KEY_RING_FILE, PLATFORM_ARTIFACT_STORAGE_ROUTE_FILE,
       PLATFORM_MODEL_GATEWAY_IMAGE_ENDPOINT,
       PLATFORM_SESSION_PROJECTION_ENDPOINT]
    outboundContracts:
      [model-gateway-image-effect-connectrpc, s3-object-api,
       session-media-projection-connectrpc]
    secretClasses:
      [platform-media-worker-database, media-operation-input-keyring,
       media-effect-capability-keyring, artifact-storage-route-registry]
    audience: platform-media-worker
    scalingKey: media-dispatch-and-cleanup-lag
    readiness: process-state
    drain: stop-media-claims-then-return-leases
    sloOwner: platform-media
    runbookOwner: platform-media
    releaseOwner: platform

  - id: platform-model-gateway
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-model-gateway
    processRole: model-gateway
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_MODEL_GATEWAY_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-model-gateway
      permissions: opaque-authorization-attempt-producer-only
      ddl: forbidden
    activationAuthorized: false
    runtimeTraffic: false
    inboundContracts: []
    declaredInboundContracts: [platform-model-gateway-connect-v1]
    outboundContracts:
      [direct-openai-chat-completions, litellm-openai-chat-completions,
       platform-model-gateway-reconciliation-https]
    secretClasses:
      [platform-model-gateway-database, mtls-server, mtls-peer-registry,
       model-gateway-response-keyring, direct-api-key, litellm-api-key]
    audience: platform-model-gateway
    scalingKey: provider-request-concurrency
    readiness: /health/ready
    drain: http2-graceful-then-abort
    sloOwner: platform
    runbookOwner: platform
    releaseOwner: platform

  - id: platform-model-image-worker
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-model-image-worker
    processRole: model-image-worker
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_MODEL_IMAGE_WORKER_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: model-image-worker
      permissions: fenced-model-image-effect-owner-only
      ddl: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    requiredEnvironment:
      [DATABASE_URL_PLATFORM, PLATFORM_DATABASE_CREDENTIAL_CLASS,
       PLATFORM_DATABASE_EXPECTED_DATABASE, PLATFORM_DATABASE_MIGRATOR_ROLE,
       PLATFORM_DATABASE_MODEL_IMAGE_WORKER_ROLE, PLATFORM_WORKER_ID,
       PLATFORM_MODEL_IMAGE_WORKER_SECRET_TRUST_ROOT,
       PLATFORM_MODEL_IMAGE_WORKER_PROVIDER_REGISTRY_FILE,
       PLATFORM_MODEL_IMAGE_WORKER_RESPONSE_KEY_RING_FILE]
    outboundContracts: [certified-image-provider-effects-v1]
    secretClasses:
      [platform-model-image-worker-database, model-image-provider-registry,
       model-image-provider-credentials, model-image-response-keyring]
    audience: platform-model-image-worker
    scalingKey: model-image-effect-dispatch-lag
    readiness: process-state
    drain: stop-model-image-claim-then-return-owned-leases
    sloOwner: platform-model
    runbookOwner: platform-model
    releaseOwner: platform

  - id: platform-authorization
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-authorization
    processRole: authorization
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_AUTHORIZATION_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-authorization
      permissions: authorization-feed-read-snapshot
      ddl: forbidden
    activationAuthorized: false
    runtimeTraffic: false
    inboundContracts: []
    declaredInboundContracts: [platform-session-authorization-connect]
    outboundContracts: []
    secretClasses:
      [
        platform-authorization-database,
        authorization-verification-keys,
        authorization-feed-cursor-key,
        mtls-server,
        mtls-peer-registry,
      ]
    audience: platform-session-authorization
    scalingKey: connect-long-poll-concurrency
    readiness: /health/ready
    drain: http2-graceful
    sloOwner: platform
    runbookOwner: platform
    releaseOwner: platform

  - id: platform-asset-data-plane
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-asset-data-plane
    processRole: asset-data-plane
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_ASSET_DATA_PLANE_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-asset-data-plane
      permissions: capability-scoped-multipart-effects-only
      ddl: forbidden
    activationAuthorized: false
    runtimeTraffic: false
    inboundContracts: []
    declaredInboundContracts: [platform-asset-upload-https]
    outboundContracts: [s3-multipart-api]
    secretClasses:
      [platform-asset-data-plane-database, tls-server, asset-upload-policy-registry,
       asset-upload-capability-keyring, asset-storage-route-registry]
    audience: platform-asset-data-plane
    scalingKey: multipart-request-concurrency
    readiness: /health/ready
    drain: https-graceful
    sloOwner: platform
    runbookOwner: platform
    releaseOwner: platform

  - id: platform-artifact-data-plane
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-artifact-data-plane
    processRole: artifact-data-plane
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_ARTIFACT_DATA_PLANE_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: artifact-data-plane
      permissions: security-definer-artifact-read-and-audit-only
      ddl: forbidden
    activationAuthorized: true
    runtimeTraffic: true
    inboundContracts: [platform-public-http]
    declaredInboundContracts: []
    outboundContracts: [s3-compatible-object-read]
    secretClasses:
      [platform-artifact-data-plane-database, tls-server, product-workload-registry,
       artifact-delivery-capability-key, artifact-private-object-route-registry]
    ports:
      - { name: public, containerPort: 4248, protocol: https, exposure: service, mtls: true }
      - { name: health, containerPort: 4249, protocol: http, exposure: pod-only, mtls: false }
    fileContract:
      trustRootEnvironment: PLATFORM_ARTIFACT_DATA_PLANE_FILE_TRUST_ROOT
      mountPath: /run/secrets/platform-artifact-data-plane
      defaultMode: "0440"
      requiredEnvironment:
        - PLATFORM_ARTIFACT_DATA_PLANE_PRODUCT_WORKLOAD_REGISTRY_FILE
        - PLATFORM_ARTIFACT_DELIVERY_CAPABILITY_KEY_FILE
        - PLATFORM_ARTIFACT_PRIVATE_OBJECT_ROUTE_FILE
        - PLATFORM_ARTIFACT_DATA_PLANE_TLS_KEY_FILE
        - PLATFORM_ARTIFACT_DATA_PLANE_TLS_CERT_FILE
        - PLATFORM_ARTIFACT_DATA_PLANE_TLS_CLIENT_CA_FILE
    probes:
      liveness: { path: /health/live, port: health, scheme: HTTP }
      readiness: { path: /health/ready, port: health, scheme: HTTP }
    audience: site-bff.artifact-delivery
    scalingKey: artifact-stream-concurrency
    readiness: /health/ready
    drain: https-stream-graceful
    sloOwner: platform
    runbookOwner: platform
    releaseOwner: platform

  - id: platform-admin
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-admin
    processRole: admin
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_ADMIN_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-admin
      permissions: typed-admin-control-plane-transactions
      ddl: forbidden
    inboundContracts: []
    declaredInboundContracts:
      [platform-admin-connect, platform-product-catalog-publication, platform-site-publication]
    activationAuthorized: false
    runtimeTraffic: false
    outboundContracts: []
    secretClasses:
      [
        platform-admin-database,
        mtls-server,
        mtls-peer-registry,
        oidc-client-registry,
        oidc-client-secret,
        oidc-transaction-encryption-key,
        admin-session-jose-key-ring,
        admin-page-cursor-key,
        commerce-redemption-keyring,
        authorization-event-signing-keyring,
        site-release-certification-verification-keyring,
        site-web-build-intent-signing-keyring,
      ]
    audience: platform-admin
    scalingKey: admin-command-concurrency
    readiness: /health/ready
    drain: http2-graceful
    sloOwner: platform
    runbookOwner: platform
    releaseOwner: platform

  - id: platform-hub-connect
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-hub-connect
    processRole: hub-connect-runtime
    inboundContracts: []
    declaredInboundContracts: [hub-runtime, hub-capability-catalog]
    outboundContracts: [platform-capability-projection]
    activationAuthorized: false
    runtimeTraffic: true
    secretClasses:
      [
        hub-mongo,
        hub-package-storage,
        hub-secret-keyring,
        capability-catalog-signing-key,
        mtls-server,
        mtls-peer-registry,
        mtls-client,
        platform-admission-peer-ca,
      ]
    audience: platform-hub-connect
    scalingKey: connect-request-concurrency
    requestTimeoutMilliseconds: 30000
    readiness: /health/ready
    drain: http2-graceful
    sloOwner: platform-hub
    runbookOwner: platform-hub
    releaseOwner: platform

  - id: platform-migrator
    command: node --conditions=kokoro-runtime deploy/docker/runtime-entrypoint.mjs
    selectorEnvironment: KOKORO_SERVICE_PACKAGE=platform-migrator
    processRole: migrator
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      apiRoleEnvironmentVariable: PLATFORM_DATABASE_API_ROLE
      admissionRoleEnvironmentVariable: PLATFORM_DATABASE_ADMISSION_ROLE
      admissionRoleLifecycle: run-scoped-leased-role
      commerceWorkerRoleEnvironmentVariable: PLATFORM_DATABASE_COMMERCE_WORKER_ROLE
      siteWorkerRoleEnvironmentVariable: PLATFORM_DATABASE_SITE_WORKER_ROLE
      assetWorkerRoleEnvironmentVariable: PLATFORM_DATABASE_ASSET_WORKER_ROLE
      adminWorkerRoleEnvironmentVariable: PLATFORM_DATABASE_ADMIN_WORKER_ROLE
      identityWorkerRoleEnvironmentVariable: PLATFORM_DATABASE_IDENTITY_WORKER_ROLE
      authorizationMaintenanceRoleEnvironmentVariable: PLATFORM_DATABASE_AUTHORIZATION_MAINTENANCE_ROLE
      authorizationRoleEnvironmentVariable: PLATFORM_DATABASE_AUTHORIZATION_ROLE
      assetDataPlaneRoleEnvironmentVariable: PLATFORM_DATABASE_ASSET_DATA_PLANE_ROLE
      artifactDataPlaneRoleEnvironmentVariable: PLATFORM_DATABASE_ARTIFACT_DATA_PLANE_ROLE
      adminRoleEnvironmentVariable: PLATFORM_DATABASE_ADMIN_ROLE
      modelGatewayRoleEnvironmentVariable: PLATFORM_DATABASE_MODEL_GATEWAY_ROLE
      memoryPublicRoleEnvironmentVariable: PLATFORM_DATABASE_MEMORY_PUBLIC_ROLE
      memoryRuntimeRoleEnvironmentVariable: PLATFORM_DATABASE_MEMORY_RUNTIME_ROLE
      memoryWorkerRoleEnvironmentVariable: PLATFORM_DATABASE_MEMORY_WORKER_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-migrator
      permissions: migration-ddl
      runtimeTraffic: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    outboundContracts: []
    secretClasses: [platform-migrator-database]
    audience: platform-migrator
    scalingKey: singleton-advisory-lock
    readiness: one-shot-exit-code
    drain: advisory-lock-release
    sloOwner: platform-data
    runbookOwner: platform-data
    releaseOwner: platform-data

  - id: platform-admin-authority-bootstrap
    command: node --conditions=kokoro-runtime dist/src/process/admin-authority-bootstrap.js --file /run/secrets/kokoro/admin-authority-bootstrap.json
    processRole: bootstrap
    database:
      environmentVariable: DATABASE_URL_PLATFORM
      expectedUserEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      migratorRoleEnvironmentVariable: PLATFORM_DATABASE_MIGRATOR_ROLE
      expectedDatabaseEnvironmentVariable: PLATFORM_DATABASE_EXPECTED_DATABASE
      credentialClass: platform-migrator
      permissions: sealed-initial-admin-authority-only
      runtimeTraffic: forbidden
    inboundContracts: []
    activationAuthorized: false
    runtimeTraffic: false
    outboundContracts: []
    secretClasses: [platform-migrator-database, initial-admin-authority-document]
    audience: platform-admin-authority-bootstrap
    scalingKey: fresh-install-singleton
    readiness: one-shot-sealed-exit-code
    drain: transaction-completion
    sloOwner: platform-security
    runbookOwner: platform-security
    releaseOwner: platform-security
