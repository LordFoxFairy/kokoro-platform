{
  "name": "@kokoro/user",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./contract": {
      "types": "./src/interfaces/http/schemas.ts",
      "import": "./src/interfaces/http/schemas.ts"
    }
  },
  "scripts": {
    "dev": "tsx src/interfaces/http/main.ts",
    "start": "tsx src/interfaces/http/main.ts",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:dev": "prisma migrate dev",
    "db:reset": "prisma migrate reset --force",
    "test": "vitest run test/unit",
    "test:integration": "vitest run test/integration --no-file-parallelism",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "coverage": "vitest run --coverage --no-file-parallelism",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@kokoro/platform-kit": "workspace:*",
    "@prisma/client": "^6.19.3",
    "fastify": "^5.10.0",
    "ioredis": "^5.4.1",
    "jose": "^5.9.6",
    "nodemailer": "^9.0.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "prisma": "^6.19.3",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
